# Very noisy but sometimes useful.
# accessLog = true

[entryPoints]
  [entryPoints.traefik]
    address = ":9000"
  [entryPoints.web]
    address = ":80"

[api]
  dashboard = true
  insecure = true

[providers]
  [providers.file]
    filename = "/traefik-routes.toml"
  [providers.etcd]
    endpoints = ["127.0.0.1:2379"]
